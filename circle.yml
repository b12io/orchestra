test:
  override:
    - make lint
    - make coverage
  post:
    - make coveralls

machine:
  python:
    version: 3.4.1

dependencies:
  override:
    - npm install -g gulp
    - npm install
    - pip install -r requirements.txt
    - pip install coveralls

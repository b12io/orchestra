test:
  override:
    - make lint
    - make coverage
  post:
    - make coveralls

machine:
  python:
    version: 3.4.1

dependencies:
  override:
    - pip install -r requirements.txt
    - pip install coveralls
